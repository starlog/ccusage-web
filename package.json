{
  "name": "cc-usage",
  "version": "1.0.0",
  "private": true,
  "description": "Collects per-user Claude Code usage reports (from the cc-usage client) and shows team statistics",
  "type": "module",
  "main": "server/src/server.js",
  "engines": {
    "node": ">=22.9"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env server/src/server.js",
    "dev": "node --env-file-if-exists=.env --watch server/src/server.js",
    "seed:sample": "node --env-file-if-exists=.env server/scripts/seed-sample.js",
    "seed:clean": "node --env-file-if-exists=.env server/scripts/seed-sample.js --clean"
  },
  "dependencies": {
    "chart.js": "^4.5.1",
    "exceljs": "^4.4.0",
    "express": "^5.2.1",
    "mongodb": "^7.6.0"
  },
  "overrides": {
    "exceljs": {
      "uuid": "^11.1.1"
    }
  }
}
